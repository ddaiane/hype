const express = require("express");
const router = express.Router();

/* router.get("/", funcao); //retornar todos apartamentos
router.get("/:sigla", funcao)/ //retorna todos apartamentos de um predio especifico
router.get("/:sigla/:codigo", funcao)/ //retorna todos apartamentos de um predio especifico
router.delete("/:sigla/:codigo", funcao); //deleta um apartamento
router.post("/", funcao); //cria apartamentos */

module.exports = router;